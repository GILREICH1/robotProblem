import React from "react";
import ReactDOM from "react-dom";
import SpiralSearch from "./components/SpiralSearch";

ReactDOM.render(
  <React.StrictMode>
    <SpiralSearch />
  </React.StrictMode>,
  document.getElementById("root")
);
